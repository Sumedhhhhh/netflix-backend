docker compose up --build
docker compose down
docker compose build --no-cache
docker compose up

# redis 
docker exec -it netflix_redis redis-cli

#postgres
docker exec -it netflix_postgres psql -U netflix